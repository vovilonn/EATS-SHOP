import AdminLayout from '@/shared/layouts/admin-layout';
import SettingsPageContent from '@/shared/admin-pages-content/settings';

const SettingsPage = () => {
  return (
    <AdminLayout>
      <SettingsPageContent />
    </AdminLayout>
  );
};

export default SettingsPage;
